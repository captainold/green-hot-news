# 服务器部署与运维 · green-hot-news

> 更新日期：2026-08-14
> 本文档记录新加坡服务器的部署架构、同步机制与运维命令，随项目一起维护。
> WSL/命令行（agent）操作服务器的方法见下方「WSL 操作服务器」章节。

## 服务器信息

- **公网IP**: 47.82.211.111（新加坡 · 阿里云轻量应用服务器）
- **域名**: ywm.life（Cloudflare CDN 代理 → 47.82.211.111）
- **系统**: Alibaba Cloud Linux 3.2104（2核 / 1.8G内存 / 40G磁盘）
- **到期**: 2027-01-31
- **SSH**: `ssh sg-moltbot`（Windows 侧别名，密钥 `~/.ssh/moltbot260130.pem`）；⚠️ WSL 下不可用，见「WSL 操作服务器」

## 部署架构

```
新加坡服务器 47.82.211.111
├── systemd timer: green-policy.timer（每30分钟）
│     └→ green-policy.service
│           └→ /opt/green-hot-news/green-policy-sync.sh
│                 ├─ 1. update_news.py 抓取 15 个源
│                 │     ├─ 官方部委/国际组织 → Notes/政策库/
│                 │     ├─ 媒体/热榜 → Notes/媒体库/（关键词过滤）
│                 │     └─ 生成 data/*.json 网站数据
│                 ├─ 2. Notes/ git commit + push → /srv/git/green-policy-materials.git
│                 └─ 3. data/ 站点数据更新
└── nginx: https://ywm.life → /opt/green-hot-news（静态站点直出）
```

### 关键路径

| 用途 | 路径 |
|------|------|
| 站点/代码根目录 | `/opt/green-hot-news/` |
| 抓取主脚本 | `/opt/green-hot-news/scripts/update_news.py` |
| 同步脚本 | `/opt/green-hot-news/green-policy-sync.sh` |
| 笔记库（独立git） | `/opt/green-hot-news/Notes/` |
| bare 仓库（同步枢纽） | `/srv/git/green-policy-materials.git` |
| API Key（权限600） | `/etc/green-policy.env` |
| 网站数据 | `/opt/green-hot-news/data/*.json` |

## 常用运维命令

```bash
# 连接服务器（Windows 侧 Obsidian Git 插件/终端）
ssh sg-moltbot

# ⚠️ WSL/命令行（agent）连服务器：WSL ssh 连不上（密钥是 Windows 专属），
#    必须用 cmd.exe 调 Windows OpenSSH —— 详细模板见下方「WSL 操作服务器」章节
```

```bash
# 查看最近抓取日志
journalctl -u green-policy.service --no-pager -n 30

# 手动触发一次抓取
systemctl start green-policy.service

# 查看定时器状态
systemctl list-timers green-policy.timer

# 查看源健康状态（16个源 ok/failed）
cat /opt/green-hot-news/data/source-status.json | jq

# 修改脚本后语法检查
cd /opt/green-hot-news && .venv/bin/python -c "import ast; ast.parse(open('scripts/update_news.py').read())"
```

## WSL 操作服务器（2026-08-14 沉淀）

**核心事实**：
1. `moltbot260130.pem` 是 Windows OpenSSH 专属密钥，**WSL 的 ssh/scp 连不上**（Permission denied）
2. `sg-moltbot` 别名只在 Windows 的 `~/.ssh/config`，WSL 里没有
3. `/opt/green-hot-news` **不是 git 仓库**——代码同步靠 **scp 推送**，不拉 GitHub
4. 本机 DNS fake-ip 劫持：一律用 IP `47.82.211.111`，不用域名 ywm.life

**模板 1：Notes 仓库 git 操作**（pull/push 到服务器）

```bash
cmd.exe /c "set GIT_SSH_COMMAND=C:/Windows/System32/OpenSSH/ssh.exe -i C:/Users/wenyu/.ssh/moltbot260130.pem -o IdentitiesOnly=yes && cd /d C:\Users\wenyu\Documents\Obsidian_wen\green-hot-news\Notes && git pull --rebase origin master"
```

**模板 2：单条远程命令**（无空格/无特殊字符 → 内联裸传；有空格/管道/引号 → 用模板 3 的 .bat）

```bash
# ✅ 已验证（无空格命令裸传）
cmd.exe /c "C:/Windows/System32/OpenSSH/ssh.exe -i C:/Users/wenyu/.ssh/moltbot260130.pem -o IdentitiesOnly=yes root@47.82.211.111 systemctl is-active green-policy.timer"
# ⚠️ 铁律：cmd.exe 不认单引号；转义双引号 \" 三层转义会失真。
#    远程命令只要含空格就写 .bat（模板 3），不要内联！
```

**模板 3：复杂命令 / scp 传文件 → 写 .bat 到项目根目录再执行**

> ⚠️ **.bat 必须纯 ASCII**（无中文注释/中文 echo）！write_file 写的是 UTF-8，cmd 按 GBK 解析中文行会乱码并连累后续所有行（报 `'""' 不是命令`）。中文说明写在 bash 命令里，.bat 内容纯英文。

```bat
@echo off
set KEY=C:/Users/wenyu/.ssh/moltbot260130.pem
set SSH=C:/Windows/System32/OpenSSH/ssh.exe
set SCP=C:/Windows/System32/OpenSSH/scp.exe
set REMOTE=root@47.82.211.111
"%SSH%" -i %KEY% -o IdentitiesOnly=yes %REMOTE% "cd /opt/green-hot-news && .venv/bin/python -c \"import ast; ast.parse(open('scripts/update_news.py').read()); print('syntax OK')\""
"%SCP%" -i %KEY% -o IdentitiesOnly=yes C:\Users\wenyu\Documents\Obsidian_wen\green-hot-news\scripts\update_news.py %REMOTE%:/opt/green-hot-news/scripts/update_news.py
"%SSH%" -i %KEY% -o IdentitiesOnly=yes %REMOTE% "md5sum /opt/green-hot-news/scripts/update_news.py"
```

**代码同步流程**：本地改脚本 → scp 上传 → 服务器语法检查 → 测试运行（`--output-dir /tmp/gpn-test` 不碰生产）→ systemd timer 每 30 分钟自动用新脚本跑（无需重启）。

**踩坑速查**：WSL ssh 直连 ✗（密钥不认）| `/c/...` 路径 ✗（git sh 不认）| cmd 单行塞复杂命令 ✗（引号嵌套炸）| cmd 单行单引号 ✗（cmd 不认单引号）| cmd 单行转义双引号 ✗（三层转义失真）| **.bat 含中文 ✗（UTF-8 被按 GBK 解析，乱码连累全文件）** | 服务器 `git pull` ✗（非 git 仓库）| `ssh sg-moltbot`（WSL）✗（别名在 Windows）| 域名 ✗（fake-ip 劫持）。判断标准：远程命令有空格就写 .bat，.bat 纯 ASCII。

## 本地 ↔ 服务器同步（Obsidian Git 插件）

### 通道配置

- **本地仓库**: `C:\Users\wenyu\Documents\Obsidian_wen\green-hot-news\Notes\`
- **origin**: `sg-moltbot:/srv/git/green-policy-materials.git`
- **Windows SSH 配置**: `C:\Users\wenyu\.ssh\config` 中别名 `sg-moltbot`
- **密钥**: `C:\Users\wenyu\.ssh\moltbot260130.pem`

### 插件配置（`.obsidian/plugins/obsidian-git/data.json`）

```json
{
  "basePath": "Notes",
  "autoSaveInterval": 15,
  "autoPullInterval": 15,
  "autoPushInterval": 30
}
```

- 服务器 → 本地：插件每15分钟 pull（服务器每30分钟抓取提交）
- 本地 → 服务器：插件每30分钟 push（Obsidian 内编辑 wiki/笔记）

### ⚠️ 注意事项

1. **本地 DNS 有代理劫持**：`ywm.life` 会被解析成 198.18.x（fake-ip），SSH/git 必须用别名 `sg-moltbot` 或 IP，不能用域名
2. **密钥权限**：Windows 侧密钥需仅当前用户可读（OpenSSH 要求），用 PowerShell 设置 ACL
3. **obsidian-git 改 basePath 后需重启 Obsidian 生效**

## 环境变量与密钥

| Key | 位置 | 用途 |
|-----|------|------|
| `ALLNET_API_KEY` | 服务器 `/etc/green-policy.env` | allnet.hot 全网热点源 |
| `ALLNET_API_KEY` | 本地项目根 `.env`（已 gitignore） | 本地运行脚本时自动加载 |

## 管理面板（消息源健康监控，不公开）

- **入口**：`https://ywm.life/admin/`（页面壳子公开；数据必须带密钥，见下）
- **访问密钥（token）**：`32578103e3ccab3bd71bfc42c7fbbf8a`（32 位 hex，本地备份 `admin/.admin-token.txt`，已 gitignore）
  - 首次访问：`https://ywm.life/admin/?key=<密钥>` → 前端自动记住（localStorage），之后直接开 `/admin/` 即可
  - 换设备/清缓存：打开 `/admin/` 会显示密钥输入框，粘贴即进
  - 任何浏览器可用（含 Electron 内核如 Wave —— 它们**不弹 Basic Auth 框**，这是弃用 Basic Auth 改用 token 的原因）
- **功能**：健康度评分、数据新鲜度徽章、48h 趋势图（正常/异常源数+绿色条目）、19 源状态表（连续失败告警）、服务器状态（timer/磁盘/git 提交）、最近运行日志；60 秒自动刷新
- **文件**：
  - 前端：`/opt/green-hot-news/admin/index.html`（单文件，零依赖，随仓库提交）
  - 数据：`/opt/green-hot-news/admin/data/health-now.json` + `health-history.json`（health_tracker.py 每次抓取后追加，保留 192 条≈96h；**生成物不提交**，本地 admin/data/ 已 gitignore）
  - 脚本：`/opt/green-hot-news/scripts/health_tracker.py`（纯 stdlib；服务器状态用 subprocess 探测 systemd/df/git，本地运行这些字段为 null 属正常）
- **鉴权架构（v2，2026-08-14）**：nginx `location ^~ /admin/data/`（最长前缀优先于 `^~ /admin/`）里 `if ($arg_key != "<TOKEN>") { return 401; }`；页面壳子 index.html 无敏感内容故公开；`access_log off` 防 key 进日志
- **踩坑记录（2026-08-14，全部实测）**：
  1. Basic Auth 在 Electron/Wave 浏览器不弹框 → 老温登不进 → 弃用
  2. nginx 1.20.1 `auth_request /xxx?key=$arg_key` 子请求返回 404（query 传递有 bug）→ 弃用 auth_request
  3. 正则 location 保护 admin/data 无效：`^~ /admin/` 前缀会**阻断所有正则评估** → 必须用 `^~ /admin/data/` 最长前缀
  4. htpasswd 权限 600 → nginx worker 读不了返回 500（需 644）——已随 Basic Auth 弃用不再需要
  5. PowerShell `Set-Content -Encoding UTF8` 写 BOM → nginx `unknown directive` → 用 WSL `sed` 生成无 BOM 配置
  6. cmd.exe 不认单引号 → .bat 远程命令别用单引号（详见 references/server-ssh-operations.md）
- **改密钥**：`openssl rand -hex 16` 生成 → 替换服务器 conf 中 `if ($arg_key != "...")` → `nginx -s reload` → 同步更新 `docs/_green-policy.conf.server` 占位 + 本地 `admin/.admin-token.txt`；前端已存 localStorage 的旧 key 需清缓存或重输
- **配置备份**：`docs/_green-policy.conf.server`（nginx 全量配置，token 用 `<ADMIN_TOKEN>` 占位，提交 git 安全）、`docs/_green-policy-sync.sh.server`（含 health_tracker 调用）——与服务器同步修改

## 历史变更

- 2026-01-30：购买服务器（新加坡轻量）
- 2026-02-05：部署 Moltbot (Clawbot/OpenClaw)，配置 nginx + 密码 + 每日监控
- 2026-03-13：OpenClaw 升级至 v2026.3.13
- 2026-08-13：**OpenClaw 彻底删除**；green-hot-news 接入 allnet.hot 全网热点源（第15个源）；本地 Obsidian Git 插件双向同步配置完成
- 2026-08-14：**管理面板上线**（`/admin/`，Basic Auth 保护）——消息源健康监控：健康度/新鲜度/48h趋势/源状态表/服务器状态/运行日志；新增 scripts/health_tracker.py，sync.sh 每次抓取后生成 admin/data/ 健康数据
