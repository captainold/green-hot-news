# 项目计划与执行监督

> 本文档追踪 green-hot-news 的**当前主线任务**的执行进度，是计划监督的单一来源。
> 阶段状态：✅ 已完成 / 🔄 进行中 / ⏳ 待执行 / ⏸ 暂停
> 每个阶段有明确的验收标准，完成后勾选。

---

## 当前主线：多维标签系统重构（2026-08-23 启动）

> 权威文档：`docs/底层数据库构建方法 260823.qmd`（老温顶层设计）
> 目标：从「树状文件夹 + 单一维度」重构为「多维属性标签 Metadata」系统。
> 三维标签：① 生命周期 Layer（含 TRL）② 国际分类 Taxonomy（EU Taxonomy/ISIC/GICS/IPC）③ 交叉技术 Enabling Tech（AI/生物/能源/环境）+ 技术特征提取（护城河）+ qmd 存储。

### ⏰ 待办提醒（迁移完成后必查）

> **历史数据 tech_feature 迁移**已完成（2026-08-23 22:30，重跑 2-3 分钟，预填充缓存优化）。
> 1. [x] 验证 data/*.json 的 `tech_feature` 字段分布（有特征/空 各多少）
> 2. [x] 抽 10 条人工核对提取质量（数值精准：50 TWh/4.25GW/262K 上下文；空样本为市场/公告/战略类，判定"无"合理）
> 3. [x] 提交 data/*.json（含 tech_feature 字段）+ tech-feature-index.json 缓存
> 4. [x] 确认 `data/tech-feature-index.json` 已生成（627 条）

### 已确认决策（2026-08-23 老温定稿）

1. dimension 命名：**中文 + 国际化双字段**（`dimension` 中文 + `layer` 国际化 Layer 1/2/3）
2. TRL：**纳入打分**（第 6 维度，来源权威 25→20 匀出 5 分）
3. 技术特征提取 LLM：**SiliconFlow 公网 API 的 DeepSeek-V4-Pro**（pro 准确率 96.7%，优于 flash 60%/chat 80%）
4. qmd：**用 qmd**（Obsidian 装插件即可显示）
5. 国际分类粒度：**EU Taxonomy + ISIC + GICS + IPC 全要**
6. 范围：**全量重做**（底层架构必须坚实）

---

## 阶段 0：本体定义 ✅ 已完成

**产出：**
- [x] `docs/本体与标签词典.md`（v1.0，多维标签系统唯一权威——三维标签词典 + 数据模型 + 触发关键词）
- [x] `docs/打分体系标准.md` → v4.0（TRL 第 6 维度，六维打分）
- [x] `docs/标签体系标准.md` → v3.0（dimension 双字段 + 多维标签）

**验收**：三维标签词典完整、触发关键词齐全、决策点全部固化 ✅

---

## 阶段 1：维度二国际分类法 ✅ 已完成

**目标**：映射 EU Taxonomy / ISIC / GICS / IPC 四大国际分类法。

- [x] `classify_eu_taxonomy(title, summary)` → 六大环境目标
- [x] `classify_isic(site_id, title, summary)` → 门类 A-U（含 SOURCE_ISIC 源映射）
- [x] `classify_gics(title, summary)` → 11 部门
- [x] `classify_ipc(title, summary)` → 8 部
- [x] item 组装写 `taxonomy = {eu_taxonomy, isic, gics, ipc}` 字段
- [x] 迁移脚本补算 taxonomy（history 865 条全部含字段）

**文件**：`scripts/update_news.py` + `scripts/migrate_dimensions.py`

**验收**：✅ 分类器测试通过；修复关键词误判（过短 it/ai、媒体源不映射）；分布 EU减缓268/ISIC信息通信235公共行政224金融103/IPC电学42

> ⏳ 服务器部署待阶段 7 统一做（历史数据迁移 + 前端展示）

---

## 阶段 2：维度三交叉技术 + TRL + layer 双字段 ✅ 已完成

**目标**：交叉技术标签（多对多）+ TRL 判定 + layer 国际化字段。

- [x] `classify_enabling_tech(title, summary)` → AI/生物科技/能源/环境（多对多）
- [x] `classify_trl(title, summary)` → TRL 档（1-3/4-6/7-9/空）
- [x] `DIM_TO_LAYER` 映射 + item 组装写 `layer` 国际化字段
- [x] `score_trl(trl)` → 第 6 维度打分（7-9档5/4-6档5/1-3档4/空3）
- [x] `score_item()` 加 trl 参数 + `score_breakdown` 加 trl 字段（六维 v4.0）
- [x] `SOURCE_SCORE` 从 25 分制压缩到 20 分制（匀 5 分给 TRL）
- [x] 迁移脚本补算 layer/trl/enabling_tech + 重打分

**文件**：`scripts/update_news.py` + `scripts/migrate_dimensions.py`

**验收**：✅ 多对多测试通过（合成生物学+碳捕集+ML筛酶 → 命中 3 标签）；TRL 判定正确；layer 分布 Layer1 312/Layer2 286/Layer3 267；三维均分 48.9/45.7/46.5 均衡

> ⚠️ 观察：SOURCE_SCORE 压缩后历史数据最高分 84（无 S 级），主因是时效衰减（老新闻 freshness 2 分）+ source 压缩。满分仍 100，新抓部委+人物+24h 新闻可达 S 级，阶段 7 部署后观察。
> ⏳ 服务器部署待阶段 7 统一做

---

## 阶段 3：技术特征提取（护城河） ✅ 已完成

**目标**：LLM 提取技术特征（降本曲线/设备参数/工艺包/API/精度）。

- [x] 评测集 `scripts/tech_feature_eval.json`（30 条标准答案：12 有特征/18 无）
- [x] 提示词模板（与老温核定 + 评测集验证）
- [x] 模型选型：**SiliconFlow DeepSeek-V4-Pro**（pro 准确率 96.7%，flash 60%/chat 80% 均不如）
- [x] `scripts/tech_feature.py`：`extract_tech_feature()`（QPS 控制 + 熔断降级）
- [x] `update_news.py` 集成：仅 Layer 2/3 提取（政策类跳过）+ tech-feature-index.json 缓存
- [x] `migrate_dimensions.py` 补算历史数据 tech_feature

**文件**：`scripts/tech_feature.py`（新）+ `scripts/update_news.py` + `scripts/migrate_dimensions.py`

**验收**：✅ 评测集 pro 准确率 96.7%（唯一争议"人形机器人焊接"纯应用场景）；功能测试通过（20GWh/26.1%/政策类"无"）

> ✅ **历史数据迁移已完成**（2026-08-23 22:30）：history 865 条 / latest-24h 322 / latest-24h-all 276 全部含 tech_feature 字段，缓存 627 条。
> 分布：有特征 49 条（5%，history）——新闻类源技术特征密度低属正常，政策类 312 条按设计跳过。
> 🔧 迁移过程修复：① 首轮后台迁移 59 分钟后被回收零写入 → 加缓存定期落盘 + 单条异常隔离 + 断点续跑；② "无"结果未缓存导致 24h 文件重复调 LLM → `_prefill_cache_from_history()` 从 history.json 预填充 483 条（含"无"），重跑 2-3 分钟完成。
> ⚠️ LLM 调用走 SiliconFlow 公网 API（key 在 .env 的 siliconflow_api_key），服务器部署时需同步到 /etc/green-policy.env

---

## 阶段 4：qmd 存储格式 ✅ 已完成

**目标**：每条信息生成 .qmd（YAML frontmatter 含多维标签）。

- [x] `scripts/export_qmd.py`（JSON → qmd 导出器，多维标签 frontmatter）
- [x] 输出 `Notes/数据库/`（扁平，靠标签多维检索）+ taxonomy 展平 4 独立字段
- [x] 实测导出（2026-08-23 22:30 用完整数据跑：history 823 + 24h 39 = 855 个 .qmd）
- [ ] Obsidian 插件配置识别 .qmd（老温侧，装插件即可）

**文件**：`scripts/export_qmd.py`（新）

**验收**：✅ 语法 + frontmatter 构建测试通过；✅ 完整导出实测通过（frontmatter 含 layer/taxonomy 4 字段/enabling_tech/tech_feature 全量多维标签）

> ⏳ 依赖阶段 3 的 tech_feature 迁移完成（数据完整后导出）

---

## 阶段 5：QA 老问题修复 ✅ 已完成

**目标**：解决抓取 QA 老问题（qa-report.md：88.5 分 B 级 → 93.5 分 A 级）。

- [x] 摘要垃圾词（补「打印本页/关闭窗口/返回顶端/网站首页/联系我们/加入收藏/登录」等页脚词）
- [x] 摘要为空 47%（`fetch_rss_feed` 补 summary/description 提取——虎嗅/36氪/澎湃等 Google News 源根因）
- [x] title_zh 作者署名残留（`translator` 清理 "✍️ Written by @xxx"）
- [x] 翻译失败残留英文（`translator.preprocess_title`：翻译前剥离 emoji/URL/@用户名——X 平台 emoji 标题翻译失败/残留根因）
- [x] 摘要截断非终止标点（qa_check B4 改长度 ≥480 才报 warn——X 推文自然结尾不算截断）
- [x] 低更新源（**重大发现：非低频，是主流程卡死**——详见下方修复记录）

**文件**：`scripts/update_news.py` + `scripts/translator.py` + `scripts/qa_check.py`

**验收**：✅ 93.5 分 A 级（err 1：FERC 偶发翻译失败回退，可接受）

### 🔧 阶段 5 期间的重大修复（2026-08-23 深夜，断源根因）

> **update_news.py 从 17:32 起每次运行卡死（8-12 分钟无产出，被 timeout 杀）→ 62 天 0 条断源假象**。
> 排查 6 小时，根因与修复：
> 1. **tech_feature LLM 提取全量串行**（对 1200+ 条非政策条目 × 3-10s/条）→ 改为**只在窗口内条目提取 + 缓存 key 从 URL 改规范化标题**（Google News URL 每次抓取不同，URL key 永不命中）+ 并发 4 + "无"也缓存 → 提取从 8+ 分钟降到 ~20 秒
> 2. **merge_history 回填 title_zh 串行翻译** → 改并发 3
> 3. **iea/irena/unfccc/worldbank 无宽窗口**（周更源被 24h/96h 窗口整源滤空）→ 加入 FOREIGN_GOV_SITES（7 天）
> 4. **bjx 查询词失效**（Google News 收录北极星少，返回 2025 旧文）→ 改多单主题词 "北极星电力网 when:30d" + "北极星 储能 when:30d"
> 5. **jiqizhixin 查询词违反 AGENTS.md 铁律**（括号 OR 语法返回 2022-2024 旧文）→ 改单主题词 + when:30d
> 6. QA 误报修复：B1 摘要空阈值放宽（Google News 源无 description 是特性）、"登录"移出垃圾词（交易语境误报）、F1 跨源去重 key 用 strip 后缀标题、summary 回填过 _clean_summary
> **结果**：update_news.py 从 8-12 分钟卡死 → **45 秒完成**（70 源 ok，984 条 history）

---

## 阶段 6：前端多维检索 ⏳ 待执行

**目标**：多维标签展示 + 交叉检索。

- [ ] 卡片展示 taxonomy / enabling_tech / trl / tech_feature
- [ ] 搜索支持 `taxonomy:` / `tech:` / `trl:` / `layer:` 语法
- [ ] 关系图谱可切换维度（Layer / 国际分类 / 交叉技术）

**文件**：`assets/app.js` + `index.html` + `assets/styles.css`

**验收**：多维标签可见、交叉检索正确命中

---

## 阶段 7：部署验证 ⏳ 待执行

**目标**：历史数据迁移 + 服务器部署。

- [ ] 扩展 `scripts/migrate_dimensions.py` 重算全部新字段
- [ ] 本地验证（抓取 + 迁移 + 分布检查）
- [ ] 服务器部署（scp + 迁移 + 触发抓取）

**文件**：`scripts/migrate_dimensions.py`

**验收**：幂等、分布均衡、服务器 70 源 ok、网站正常

---

## 依赖关系

```
阶段 0（本体）✅ → 阶段 1（国际分类）→ 阶段 2（交叉技术+TRL+layer）→ 阶段 3（技术特征）
                                     ↘ 阶段 4（qmd）依赖阶段 1-3 的字段
                                     ↘ 阶段 5（QA）独立，可并行
阶段 6（前端）依赖阶段 1-3 字段确定
阶段 7（部署）依赖阶段 1-6 完成
```

**建议执行顺序**：阶段 1 → 2 → 3（串行，同文件 update_news.py）→ 阶段 4/5（并行）→ 阶段 6 → 7

---

## 执行纪律

1. **每阶段完成后**更新本文档的 checkbox 状态 + 记录验收结果
2. **打分/分类改动**必须同步 `docs/打分体系标准.md` + `docs/本体与标签词典.md` + `scripts/update_news.py`（AGENTS.md 铁律）
3. **提示词/评测集**（阶段 3）需老温亲自核定，不可自行决定
4. **每阶段独立 commit**，便于回滚
